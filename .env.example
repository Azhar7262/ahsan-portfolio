# SMTP email (optional) — if not set, form messages are saved to data/messages.json
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Public site URL (used for canonical/OG tags)
SITE_URL=http://localhost:3000
